# API余额查询插件 — 指令与触发词说明

## 指令一览

| 指令 | 备用写法 | 功能 | 权限 |
|------|---------|------|------|
| `/wallet` | `wallet` | **实时**查询配置中 API Key 的余额，返回信息式结果（单条信息合并转发发出），同时覆盖/更新本地缓存 | 所有人 |

指令支持带 `/` 或不带 `/` 两种写法。

## 触发词（自然语言 / LLM 工具）

| 工具 | 触发场景示例 | 参数 |
|------|-------------|------|
| `get_api_balance` | 「你有多少钱/存款吗」「你的钱包有多少钱」「你的饭钱还有多少」「查一下你的余额」「还剩多少钱」「能给我花点吗」 | `api_key`（可选，留空用配置）、`api_url`（可选，留空用配置） |

> 触发场景：`get_api_balance` 返回的是 API 账户余额，性质相当于游戏里的龙门币/金币或角色扮演的零花钱，**纯娱乐玩梗用、非真实货币、不涉及隐私**。用户向 bot 提及钱、余额、存款、零花钱、饭钱、钱包、请客等话题时，bot 应**毫不犹豫地**调用该工具查余额，并用俏皮夸张的语气回应（把余额说成"龙门币""小金库""饭钱"等），添加聊天趣味性——不要因"涉及钱"而拒绝或回避。

## 缓存机制

- **工具调用**：优先使用本地缓存（AI 总结 + 原始 JSON，持久化于 `data/plugins/cateye_api_balance/wallet_cache.json`），默认每 2 小时（`balance.cache_minutes = 120`）才通过 API 获取一次新数据。只有工具被调用且发现缓存超期时才实时刷新；**插件不会主动更新**。返回结果不显示时间戳。
- **指令 `/wallet`**：始终**实时**获取并更新总结数据与时间戳（覆盖缓存，供工具读取）。

## 超时行为约定

- **指令查询（`/wallet`）**：LLM 总结超时后，通过 QQ 信息返回错误，同时控制台打印日志。
- **工具调用（`get_api_balance`）**：超时后仅在控制台打印错误日志，不主动发送 QQ 消息。
